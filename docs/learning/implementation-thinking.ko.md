# 구현 사고

## 이 개념은 무엇인가
이 페이지는 구현 사고 개념을 코딩 테스트 맥락에서 빠르게 이해하기 위한 학습 노트입니다.

## 언제 사용하는가
입력 크기와 요구 조건을 보고, 반복 계산을 줄이거나 탐색 전략을 정해야 할 때 사용합니다.

## 핵심 아이디어
문제를 바로 코드로 옮기기보다, 자료구조·인덱스·반복 구조를 먼저 설계하면 실수를 줄일 수 있습니다.

## 기본 문법 또는 패턴
```python
# core pattern skeleton
# adapt to this topic
```

## 단계별 예시
1. 입력/조건을 확인합니다.
2. 이 개념이 필요한 신호를 찾습니다.
3. 안전한 패턴으로 작은 예제를 검증합니다.
4. 경계 케이스를 점검한 뒤 구현합니다.

## 흔한 실수
- 인덱스 기준(1-based/0-based)을 섞어서 사용하는 실수
- 시간복잡도(time complexity)를 계산하지 않고 구현부터 시작하는 실수
- 중간 상태를 검증하지 않고 한 번에 완성하려는 실수

## 안전한 패턴
- 먼저 불변 조건(invariant)을 정리합니다.
- 작은 입력으로 수작업 결과와 코드 결과를 비교합니다.
- 최종 구현 전에 시간복잡도와 공간복잡도(space complexity)를 다시 계산합니다.

## 시간복잡도
문제 조건에 따라 다르므로, 이 개념을 적용한 뒤 최악 케이스를 기준으로 계산합니다.

## 관련 문제
- 관련된 문제를 학습 순서에 맞게 연결하세요.

## 복습 체크리스트
- 왜 이 개념이 필요한지 한 문장으로 설명할 수 있는가?
- 안전한 패턴을 코드로 재현할 수 있는가?
- 경계 케이스를 스스로 만들고 검증했는가?
