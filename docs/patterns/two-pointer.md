# Two Pointer

Two pointers are helpful when a sequence is sorted or when a window can expand and shrink.

## Checklist

- State what each pointer means.
- Move only the pointer that makes progress toward the invariant.
- Avoid infinite loops by changing at least one pointer each step.

## Python hint

For sliding windows, update the answer after the window satisfies the intended condition.
