# Hash

Use dictionaries and sets when lookup speed matters.

## Checklist

- Decide the key before coding.
- Store only the information needed for later decisions.
- Watch for duplicate keys and update rules.

## Python hint

`dict.get(key, default)` is useful for counting or grouping.
