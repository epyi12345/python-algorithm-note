# Sort

Sorting can turn a hard search into a simple scan.

## Checklist

- Choose the sort key explicitly.
- Confirm whether ascending or descending order is needed.
- After sorting, look for adjacent relationships.

## Python hint

Use `key=` instead of changing the original data shape only for sorting.
