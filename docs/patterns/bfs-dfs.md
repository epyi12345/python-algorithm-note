# BFS/DFS

Use graph traversal to explore connected states.

## Checklist

- Mark visited nodes when they are added, not after repeated discovery.
- Pick BFS for shortest paths in unweighted graphs.
- Pick DFS for exhaustive exploration or component checks.

## Python hint

`collections.deque` is the usual queue for BFS.
